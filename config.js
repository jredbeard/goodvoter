let config = {};

// voters to follow
config.followed = {
    voters: ['c-cubed', 'justinw'],
}

// vote weight
config.voteWeight = {
    'c-cubed': 9000,
    'justinw': 9000,
}

// target voting mana in percent
config.targetMana = 75;

module.exports = config;